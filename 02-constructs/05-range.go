package main

import "fmt"

func main() {

	/*num := [5]int{100, 207, 360, 140, 530}

	for index, value := range num {
		fmt.Println(index, value)
	}*/

	num := []int{100, 207, 360, 140, 530}

	for index, value := range num {

		fmt.Println(index, value)
	}
}
